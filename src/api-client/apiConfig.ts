import { Configuration } from '../api/generated';

export const apiConfig = new Configuration({
  basePath: 'http://localhost:8080',
  // You can add more config options here (e.g., accessToken, headers)
});
